version: 1
frontend:
  phases:
    preBuild:
      commands:
        - corepack enable
        - corepack prepare pnpm@latest --activate
        - pnpm install
    build:
      commands:
        # https://docs.aws.amazon.com/amplify/latest/userguide/ssr-environment-variables.html
        - touch .env.production
        - env | grep -e BETTER_AUTH >> .env.production
        - env | grep -e GITHUB >> .env.production
        - env | grep -e DATABASE_ >> .env.production
        - env | grep -e DATA_ENCRYPTION_KEY >> .env.production
        - pnpm run build
  artifacts:
    baseDirectory: .next # or dist, out, etc.
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .pnpm-store/**/*
      - .next/cache/**/*
